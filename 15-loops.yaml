- name: loops
  hosts: localhost
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" # item is reserved
    loop:
    - Ramesh
    - suresh
    - raheem
    - john